# The client talks to the backend through Vite's dev proxy (see vite.config.js),
# so no API URL is required for local development.
# For a production build pointing at a remote API, configure your reverse proxy
# to forward /api and /socket.io to the backend host.
