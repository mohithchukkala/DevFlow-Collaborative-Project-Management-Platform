# Local development: leave VITE_API_URL unset — the Vite dev server proxies
# /api and /socket.io to the backend (see vite.config.js).

# Production (Vercel): set this to your deployed backend origin, NO trailing /api.
# Example:
# VITE_API_URL=https://your-backend.up.railway.app
